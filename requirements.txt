# FastAPI web framework
fastapi>=0.111,<1.0

# ASGI server; 
uvicorn[standard]>=0.30,<1.0

# Required for file upload / form data support
python-multipart>=0.0.9,<1.0

# Typed settings from env/.env
pydantic>=2.6,<3.0
pydantic-settings>=2.2,<3.0

# Simple structured logging
loguru>=0.7,<1.0

# NumPy 2.x — required for Python 3.13 wheels. Torch 2.5+ and
# sentence-transformers 4.x are NumPy 2-compatible, so the old ABI concern
# (pin <2) no longer applies.
numpy>=2.1,<3.0

# to run tests
pytest>=8.2,<9.0
httpx>=0.27,<1.0
beautifulsoup4>=4.12,<5.0
pypdf>=4.3,<6.0
python-docx>=1.1,<2.0

# Excel file support (.xlsx)
openpyxl>=3.1,<4.0

# Legacy Excel (.xls) support (optional fallback)
xlrd>=2.0,<3.0

# RTF file support (optional)
striprtf>=0.0.26,<1.0

# Arabic/RTL PDF extraction via pdfplumber.
# pdfplumber provides word-level bounding boxes, enabling position-based
# right-to-left word ordering for Arabic PDFs (words stored in visual order
# in the content stream). Falls back to pypdf + NFKC normalization.
pdfplumber>=0.10,<1.0

# Self-hosted OCR via Tesseract (Arabic + English).
# Requires system packages: tesseract, poppler (for pdf2image).
# Install Arabic data: download ara.traineddata to tessdata dir.
pytesseract>=0.3,<1.0
pdf2image>=1.16,<2.0
Pillow>=10.0,<13.0

# Back-end. macOS wheels are CPU-only by nature; Linux/Windows users who want
# the CPU-only variant should add `--extra-index-url https://download.pytorch.org/whl/cpu`
# to their pip install command. 2.5+ ships Python 3.13 wheels.
torch>=2.5,<3.0

# Ai model loader (>=4.1: SentenceTransformerTrainer.compute_loss accepts num_items_in_batch
# from transformers 4.5x; ST 3.0.1 raises TypeError with recent transformers)
sentence-transformers>=4.1.0,<5.0.0

# Required for sentence-transformers model.fit() (Trainer uses huggingface datasets)
datasets>=2.16,<4.0

# Gemini LLM verification (Phase 1)
google-generativeai>=0.7,<1.0

# --- BGE fine-tuning pipeline (Phase 2) ---
# Direct PostgreSQL access for training data queries
psycopg2-binary>=2.9,<3.0

# Training optimization (fp16, gradient accumulation). 1.x required for transformers 4.5x
# (unwrap_model(..., keep_torch_compile=...) API).
accelerate>=1.0,<2.0

# Progress bars for long-running scripts
tqdm>=4.66,<5.0

# HTTP requests for MOJ API scraping
requests>=2.31,<3.0

python-multipart>=0.0.9
